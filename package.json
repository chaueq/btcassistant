{
  "name": "BTCassistant",
  "version": "1.8.4",
  "description": "Simple assistant for BTC traders",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . BTCassistant --platform=win32 --arch=x64 --out dist/ --overwrite && electron-packager . BTCassistant --platform linux --arch x64 --out dist/ --overwrite && electron-installer-debian --src dist/BTCassistant-linux-x64/ --dest dist/installers/ --arch amd64",
    "install": "sudo dpkg -i dist/installers/btcassistant_1.0.0_amd64.deb"
  },
  "author": "chaueq",
  "license": "Specified in LICENSE.md",
  "devDependencies": {
    "electron": "^13.1.6",
    "electron-installer-snap": "^5.1.0"
  }
}
